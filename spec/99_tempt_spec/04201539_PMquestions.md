你不要进行任何执行，只进行规划、回答问题，并将回答的结果输出到Log中。
我现在对项目管理有疑问，目前对该项目中tempt、mainpipeline、subpipeline是这样理解的：- 1. E:\00_Commute_Scenario_Research\notebooks\sub_pipeline中进行函数调用以满足分支任务、不同于主流程步骤，或者已经具备中间结果而输入中间结果无需从头跑主流程的任务情况，可以理解为取主流程的片段流程运行。分支流程的notebooks中小标题的起名跟main_pipeline起名一致，分支流程的输出结果仍然是论文的主体结果，放在results文件对应的小标题之下。
- 2. E:\00_Commute_Scenario_Research\notebooks\02_eda_and_stats.ipynb其中是的函数调用输出次要数据的，但是论文中仍会用这些次要结果
除了特别申明的临时需求运行及sub_pipeline之外，对调用函数的流程有改动就要更新到`notebooks/01_main_pipeline.ipynb`该文件中，且按照其步骤规范地放到相应子文件夹之下。
修改调用函数的流程之后，还需要测试是否能输出符合预期的结果。
文件存放声明
E:\00_Commute_Scenario_Research\spec\99_ad-hoc_requirements临时需求的描述文档放在该文件夹中，临时需求包含测试需求、重新跑结果检查正确性的需求等，对应在E:\00_Commute_Scenario_Research\tempt中调用函数执行，结果也放在E:\00_Commute_Scenario_Research\results\tempt中进行函数调用，临时需求的输出论文不会有引用
即临时spec→tempt调用流程→结果存放到对应的tempt文件夹现在对main_pipeline.ipynb的小标题也进行了调整
对一些文件的命名也做了调整。99_ad-hoc_requirements改名为99_tempt_spec，tempt的输出结果不再放进总的results文件夹，而放在"E:\00_Commute_Scenario_Research\tempt\tempt_results"，不跟文章引用的数据结果放在一起


但是，我有疑问：
1.对主流程和分支流程的疑惑。一般来说软件工程中的主流程是可以从头运行到尾的吗？如果我只想运行其中一部分但是这样调用又不方便，比如主流程输入的是原始数据，但其实我已经有中间数据出来了，就不方便直接跑主流程，因为此时变量是没有中间结果的。但是每次因为局部增补就去写分支流程也很麻烦，这个分支流程说到底也只是主流程的切片，从主流程的中间步骤开始运行。遇到这类窘境请问怎么办呢？mainpipeline的开关主要是怎么实现的，需要承继的中间变量的设计吗？可以这样理解吗？mainpipeline是全局调用，涉及到跨模块的。subpipeline是小模块/阶段的，相当于对函数的封装。那么各个小标题中的结果输出是否不应该放到subpipeline封装？这样的话，比如按阶段将情景一的刚性标定、情景推演和结果分析全部封装成了一个subpipeline，那么在mainpipeline中调用起到的作用仅仅是一个文件将所有的结果输出写好了。
 2.关于结果测试，tempt的log，tempt对于函数、流程分支的分层，tempt存在一个log的子文件夹中，实际运行存在另外一个log的子文件夹中。tempt实质上应该是对各个集成层级的代码有不同层次的调用。我现在要进行的事情是新增评估的功能，我需要先写函数（做函数检验）、再写评估模块（做针对该subpipeline的检验），最后任意在pipeline的一级小标题3.Situation_Diagnosis和4.Scenario_Computation中有微差的调用，这整个改写网络开源代码-测试-集成subpipeline-调用的过程应该如何安排？
3.封装了一个subpipeline怎么个性化调用，比如我论文中各阶段的结果都需要评估，很显然要做一个评估的subpipeline，但是，每个阶段的评估在大体上相同但具体要出的图又有微差。即将封装一整个评估的模块，但是这个模块中的指标层级不是全调用的，比如把所有的评估项打一个编号，然后调用时指出不用的编号吗？
4.我发现出图的代码经常有问题，所以出图的函数其实需要我检查结果之后再确定。

我的时间不多了，请你结合时间紧迫性帮我想想这些问题、项目架构管理、以及如何安排时间优先级，我的最终目的不是架构多清楚，而是出论文，保证论文结果层次清晰、评估板块能在现状格局、未来情景分析中调用就行了。最终论文结果就需要引用star开头的图表等并按照章节组织在一起。你不要进行任何执行，只回答问题，并将回答的结果输出到Log中。
