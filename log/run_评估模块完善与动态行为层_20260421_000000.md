# Run Log — 评估模块完善与动态行为层搭建 2026-04-21

## 任务概述

完成两大任务：
1. **修改已有图表**（静态结构层、优化潜力层）：5项修改需求
2. **新增动态行为层**（3.1.2Dynamic_Stats）：时间、交通方式、OD流可视化、社区发现

## 输入数据

- 完整OD数据：`data/[主城区]TAZ4-od.csv`（507.9 MB，含时间、5种交通方式比例）
- 街道边界：`F:\02_250910_Commute\02-原始数据\[最终矫正版]街道乡镇边界\31\内部边界修改.shp`
- Mapbox token：通过环境变量 `MAPBOX_TOKEN` 读取（不写入仓库）

## 执行步骤

### Phase 1: 规范文档编写

1. **spec/99_tempt_spec/0421_修改旧图需求.md**
   - 5项图表修改详细规范
   - 标准差椭圆线宽×2 + 说明文档
   - 平衡度图例修改 + 街道级平衡度
   - 人数分布图8级分类
   - TAZ空白底图
   - 四格局人流箱线图（双子图）

2. **spec/05_subpipeline_spec/01_dynamic_behavior_layer.md**
   - 动态行为层完整规范
   - 新增函数签名和实现要点
   - 输出文件清单
   - 验证清单

### Phase 2: 源代码修改

#### src/config.py
- `balance_ratio` unit: `'倍'` → `''`（移除"倍"字）
- `static_people` bins: 5级 → 8级 `[0, 500, 1000, 2000, 4000, 8000, 12000, 16000, 20000]`
- `static_people` colors: 5种 → 8种颜色
- 新增 `self_sufficiency_density` 配色方案（单位：1/km²）
- 新增 `avg_time` 配色方案（单位：分钟）

#### src/geo_excu.py
- `plot_std_ellipse()` linewidth: 2.5 → 5.0（第152行、158行）
- 新增 `plot_std_ellipse_flow()` 函数（基于OD流的标准差椭圆）
- 新增 `community_detection_tbd()` 函数（igraph Louvain社区发现）

#### src/metrics_eval.py
- 修改 `compute_street_self_sufficiency()` 增加面积归一化
  - 计算街道面积（km²）
  - 新增 `自给度密度 = 自给度 / 面积_km2`
- 新增 `compute_time_indicators()` 函数（TAZ级时间 + 全局时长分段）
- 新增 `compute_transport_mode_stats()` 函数（5种交通方式加权占比）
- 新增 `compute_street_balance_ratio()` 函数（街道级职住平衡度）

#### src/visualization.py
- 新增 `create_pie_chart()` 函数（时长区间、交通方式饼图）
- 新增 `create_blank_taz_map()` 函数（空白TAZ底图）
- 新增 `create_od_flowmap_tbd()` 函数（transbigdata交互式OD流线图）
- 新增 `html_to_png()` 函数（playwright HTML截图转PNG）

### Phase 3: 评估模块更新

#### notebooks/sub_pipeline/evaluation.ipynb
- 修改 `eval_V98_flow_boxplot()` 为双子图
  - 左图：0-200人细节视图
  - 右图：对数轴全貌（上限2000人）
- 新增动态行为层评估函数：
  - `eval_M9_V9_time_distribution()` — 时间指标 + TAZ地图 + 饼图
  - `eval_M10_V10_transport_mode()` — 交通方式统计 + 饼图
  - `eval_V11_od_flowmap()` — OD流线图（HTML + PNG）
  - `eval_V12_community_detection()` — 社区发现（HTML + PNG + TXT报告）
  - `eval_V13_std_ellipse_flow()` — 通勤流标准差椭圆
- 更新 `EVAL_REGISTRY` 和 `EVAL_CONFIGS`
  - 新增 `dynamic_behavior` 配置
- 更新 `run_evaluation()` 函数签名
  - 新增参数：`df_od_full`, `mapbox_token`, `bounds`, `zoom`, `style`

### Phase 4: 测试笔记本创建

1. **tempt/0421_修改旧图.ipynb**
   - 测试5项图表修改
   - 生成v3/v4版本图片
   - 生成标准差椭圆说明文档
   - 验证清单

2. **tempt/0421_动态行为层测试.ipynb**
   - 测试所有动态行为层函数
   - 生成时间、交通方式指标
   - 生成OD流线图和社区发现可视化
   - 生成汇总CSV

### Phase 5: 主流程集成

#### notebooks/01_main_pipeline.ipynb
- 在 `3.1.2Dynamic_Stats` 节新增代码cell
- 加载完整OD数据（`data/[主城区]TAZ4-od.csv`）
- 调用 `run_evaluation()` 执行动态行为层评估
- 传入 Mapbox token 和地图范围参数

## 输出文件

### 修改旧图（v3/v4版本）
- `3.1.1Static_Stats/star_标准差椭圆_v3.png` — 线宽5.0
- `3.1.1Static_Stats/star_标准差椭圆_说明.md` — 椭圆参数说明
- `3.1.1Static_Stats/star_平衡度分布图_v3.png` — 图例无"倍"字
- `3.1.1Static_Stats/star_平衡度分布图_街道_v3.png` — 街道级平衡度
- `3.1.1Static_Stats/star_人数分布_O端_v3.png` — 8级分类
- `3.1.1Static_Stats/star_人数分布_D端_v3.png` — 8级分类
- `3.1.1Static_Stats/star_TAZ研究单元底图.png` — 空白底图
- `3.1.3Pattern_Comparison/star_格局对比_人流箱线图_四格局_v3.png` — 双子图

### 动态行为层（新增）
- `3.1.2Dynamic_Stats/star_平均通勤时间分布图_TAZ.png` — TAZ级时间地图
- `3.1.2Dynamic_Stats/star_通勤时长区间占比_饼图.png` — 5个时段占比
- `3.1.2Dynamic_Stats/star_交通方式占比_饼图.png` — 5种方式占比
- `3.1.2Dynamic_Stats/star_OD流线图_实际格局.html` — 交互式流线图
- `3.1.2Dynamic_Stats/star_OD流线图_实际格局.png` — 静态截图
- `3.1.2Dynamic_Stats/star_社区发现结果.html` — 社区可视化
- `3.1.2Dynamic_Stats/star_社区发现结果.png` — 静态截图
- `3.1.2Dynamic_Stats/star_社区发现_模块度报告.txt` — 模块度统计
- `3.1.2Dynamic_Stats/star_动态行为指标汇总.csv` — 时间+交通方式汇总
- `3.1.2Dynamic_Stats/star_标准差椭圆_通勤流.png` — 基于OD流的椭圆

## 关键技术实现

### 1. 标准差椭圆线宽调整
```python
# src/geo_excu.py, line 152, 158
ax.plot(ell_home['ellipse_x'], ell_home['ellipse_y'],
        color='#2E7D9A', linewidth=5.0, label='居住地（O端）')
ax.plot(ell_work['ellipse_x'], ell_work['ellipse_y'],
        color='#C65A4A', linewidth=5.0, linestyle='--', label='工作地（D端）')
```

### 2. 街道级自给度面积归一化
```python
# src/metrics_eval.py
gdf_out['面积_km2'] = gdf_out.geometry.area / 1e6
gdf_out['自给度密度'] = np.where(
    gdf_out['面积_km2'] > 0,
    gdf_out['自给度'] / gdf_out['面积_km2'],
    np.nan
)
```

### 3. 双子图箱线图
```python
# notebooks/sub_pipeline/evaluation.ipynb
fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(24, 7))
# 左图：0-200细节
ax1.boxplot(data_list_left, ...)
# 右图：对数轴全貌
ax2.boxplot(data_list_right, ...)
ax2.set_xscale('log')
```

### 4. 时长分段统计
```python
# src/metrics_eval.py
bins = [0, 900, 1800, 2700, 3600, float('inf')]  # 秒
labels = ['<15分钟', '15-30分钟', '30-45分钟', '45-60分钟', '>60分钟']
df['时长段'] = pd.cut(df[time_col], bins=bins, labels=labels)
```

### 5. 交通方式加权统计
```python
# src/metrics_eval.py
mode_cols = {'驾车比例': '驾车', '地铁比例': '地铁', ...}
for col, name in mode_cols.items():
    weighted = (df[col] * df[value_col]).sum() / total
    result[name] = round(float(weighted), 4)
```

### 6. 社区发现（Louvain算法）
```python
# src/geo_excu.py
import igraph as ig
g = ig.Graph(n=len(all_taz), edges=edges, directed=False)
g.es['weight'] = weights
g_clustered = g.community_multilevel(weights=g.es['weight'])
modularity = g_clustered.modularity
```

## 依赖安装

```bash
pip install transbigdata igraph seaborn playwright
playwright install chromium
```

## 验证清单

- [x] 规范文档编写完成（2个spec文件）
- [x] 源代码修改完成（4个源文件）
- [x] 评估模块更新完成（evaluation.ipynb）
- [x] 测试笔记本创建完成（2个测试notebook）
- [x] 主流程集成完成（01_main_pipeline.ipynb）
- [ ] 运行测试笔记本生成图表（需用户手动执行）
- [ ] 验证所有输出文件存在

## 后续步骤

1. 运行 `tempt/0421_修改旧图.ipynb` 生成v3/v4版本图表
2. 运行 `tempt/0421_动态行为层测试.ipynb` 测试动态行为层功能
3. 安装依赖：`pip install transbigdata igraph playwright && playwright install chromium`
4. 运行主流程 `notebooks/01_main_pipeline.ipynb` 完整执行
5. 验证所有输出文件完整性

## 注意事项

- 原图（v2版本）已保留，新图加v3/v4后缀
- 完整OD数据（507.9 MB）加载需要时间
- transbigdata + Mapbox 需要网络连接
- HTML转PNG需要playwright和chromium浏览器
- 社区发现计算量较大，建议使用top_n限制OD对数

## 文件修改统计

- 修改文件：6个（config.py, geo_excu.py, metrics_eval.py, visualization.py, evaluation.ipynb, 01_main_pipeline.ipynb）
- 新增文件：4个（2个spec + 2个测试notebook）
- 新增函数：11个
- 修改函数：3个

## 完成时间

2026-04-21 00:00:00
