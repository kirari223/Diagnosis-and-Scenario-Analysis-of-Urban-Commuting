# 运行日志 - 评估模块搭建（静态结构层 + 优化潜力层）

## 基本信息
- 执行时间：2026-04-20 22:00-22:30
- 执行人：Claude Sonnet 4.6
- 任务：搭建评估模块，运行静态结构层和优化潜力层

---

## 输入数据

| 数据 | 路径 |
|------|------|
| 实际格局 | results/1.Data_Preprocess/实际格局-统一结构.csv |
| 理想格局 | results/2.Pattern_Computation/2.1Theoretical_Pattern/理想格局-统一结构-0414.csv |
| 基准格局 | results/2.Pattern_Computation/2.4Baseline_Int/star_baseline_int.csv |
| 随机格局 | results/2.Pattern_Computation/2.5Random_Int/star_random_int.csv |
| TAZ边界 | data/TAZ4_shapefile4326.shp |
| TAZ边界(投影) | data/TAZ4_shapefile2427.shp |
| 静态分布 | data/[主城区]TAZ4-static.csv |
| 街道边界 | F:\02_250910_Commute\02-原始数据\[最终矫正版]街道乡镇边界\31\内部边界修改.shp |

---

## 新增代码

### src/geo_excu.py（新文件）
- `compute_std_ellipse(coords, weights)` — 计算加权标准差椭圆参数
- `plot_std_ellipse(fence, static_csv_path, output_path)` — 绘制O/D端标准差椭圆图

### src/metrics_eval.py（追加函数）
- `compute_balance_ratio(static_csv_path, fence)` — 计算TAZ职住平衡度
- `compute_street_self_sufficiency(df_od, fence, street_shp_path)` — 计算街道级自给度
- `compute_excess_commute(c_obs_km, c_min_km, c_ran_km)` — 计算超额通勤指标

### src/visualization.py（追加函数）
- `create_street_choropleth(gdf_street, gdf_taz_boundary, column, config_key, output_path)` — 街道级分级设色图

### src/config.py（追加配色方案）
- `balance_ratio` — 职住平衡度配色（蓝-白-红发散）
- `self_sufficiency` — 自给度配色（绿色序列）
- `static_people` — 静态人数分布配色（红色序列）

---

## 执行步骤

### 静态结构层（3.1.1Static_Stats）

1. V1 人数分布图（O/D端）
   - 按起点/终点聚合实际格局人数
   - 调用 `create_choropleth_map()` 出图
   - 输出：star_人数分布_O端.png, star_人数分布_D端.png

2. M3/V3 平衡度
   - 调用 `compute_balance_ratio()` 从 static.csv 计算 work/home 比值
   - 调用 `create_choropleth_map()` 出图
   - 输出：star_平衡度分布图.png, star_平衡度指标.csv

3. M4/V4 街道自给度
   - 调用 `compute_street_self_sufficiency()` 将TAZ中心点关联到街道，计算内部通勤比
   - 调用 `create_street_choropleth()` 出图（外边界TAZ2427，内部街道分色）
   - 输出：star_自给度分布图_街道.png, star_自给度指标_街道.csv

4. V92 标准差椭圆
   - 调用 `plot_std_ellipse()` 对home/work人口分别计算加权椭圆
   - 输出：star_标准差椭圆.png

### 优化潜力层（3.1.3Pattern_Comparison）

5. M4-M7 超额通勤指标
   - C_obs = 5.5776 km（实际格局加权平均通勤距离）
   - C_min = 1.118 km（理想格局），C_ran = 13.144 km（随机格局）
   - EC=79.96%, NEC=37.08%, CE=57.57%, NCE=62.92%
   - 输出：star_超额通勤指标.csv

6. V98 人流箱线图（实际 vs 理想/基准/随机）
   - 调用 `create_distribution_plot()`，截尾 cap=20
   - 输出：star_格局对比_人流箱线图_*.png（3张）

7. V97 距离pdf对比图（实际 vs 理想/基准/随机）
   - 调用 `create_distance_pdf()`，截尾 20km，加权KDE
   - 输出：star_格局对比_距离pdf_*.png（3张）

8. V96 静态分布人数差值图（实际 vs 理想/基准/随机）
   - 调用 `compute_taz_indicators()` + `compute_diff()` + `create_diff_maps()`
   - 输出：map_diff_总通勤人数_*.png（3张）

9. V95 OD流量差值流线图（实际 vs 理想/基准/随机）
   - 两格局OD数据outer join后计算差值列
   - 调用 `create_flowline(is_diff=True)`，top_n=500
   - 输出：OD流量差_*.png（3张）

---

## 输出文件

### 3.1.1Static_Stats（7个文件）
- star_人数分布_O端.png（3866KB）
- star_人数分布_D端.png（3869KB）
- star_平衡度分布图.png（3800KB）
- star_平衡度指标.csv（142KB，4817行）
- star_标准差椭圆.png（1929KB）
- star_自给度分布图_街道.png（3724KB）
- star_自给度指标_街道.csv（5KB，110行）

### 3.1.3Pattern_Comparison（13个文件）
- star_超额通勤指标.csv
- star_格局对比_人流箱线图_实际格局vs理想格局.png（72KB）
- star_格局对比_人流箱线图_实际格局vs基准格局.png（73KB）
- star_格局对比_人流箱线图_实际格局vs随机格局.png（74KB）
- star_格局对比_距离pdf_实际格局vs理想格局.png（217KB）
- star_格局对比_距离pdf_实际格局vs基准格局.png（284KB）
- star_格局对比_距离pdf_实际格局vs随机格局.png（268KB）
- map_diff_总通勤人数_实际格局_vs_理想格局.png（2744KB）
- map_diff_总通勤人数_实际格局_vs_基准格局.png（3567KB）
- map_diff_总通勤人数_实际格局_vs_随机格局.png（3494KB）
- OD流量差_实际格局vs理想格局.png（1756KB）
- OD流量差_实际格局vs基准格局.png（1664KB）
- OD流量差_实际格局vs随机格局.png（1650KB）

---

## 关键数值

| 指标 | 值 |
|------|-----|
| C_obs（实际格局平均通勤距离） | 5.5776 km |
| C_min（理想格局） | 1.118 km |
| C_ran（随机格局） | 13.144 km |
| EC（超额通勤率） | 79.96% |
| NEC（标准化超额通勤率） | 37.08% |
| CE（通勤经济） | 57.57% |
| NCE（标准化通勤经济） | 62.92% |
| 平衡度均值 | 4.137 |
| 平衡度中位数 | 1.281 |
| 街道自给度均值 | 0.148 |

---

## 注意事项

1. 理想格局列名为 Htaz/Jtaz/flow/distance_m，运行时已重命名为 o/d/人数/distance
2. 街道边界CRS为EPSG:32649，TAZ4326为WGS84，函数内部已自动统一CRS
3. 街道自给度：110个街道中92个有有效数据，18个街道无TAZ中心点落入（边界外）
4. 平衡度均值4.137偏高，因部分TAZ居住人数极少但工作人数较多（就业中心）
5. transbigdata已安装，OD流线图本次使用matplotlib静态版本（create_flowline），交互式版本后续可用transbigdata实现
